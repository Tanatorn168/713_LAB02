export const add = (a: number, b: number): string => {
    const result = a + b;
    return result.toString();
}

export const substract = (a:number,b:number) : string => {
    const result = a - b;
    return result.toString();
}